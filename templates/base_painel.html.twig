{% extends 'base.html.twig' %}
{% block title %}Painel do usuário - {% endblock %}
{% block body %}
    <div class="container mt-5">
        {% block topo_painel %}
        {% endblock %}
        <hr>
        <div class="row mt-5">
            <div class="col-md-3">
                <div class="list-group list-group-flush">
                    <!-- TODO: FAZER FUNCIONAR CLASS ACTIVE -->
                    <a href="#" class="list-group-item list-group-item-action">Meus microjobs</a>
                    {% if is_granted("ROLE_CLIENTE") %}

                    {% endif %}
                    {% if is_granted("ROLE_FREELA") %}

                    {% endif %}

                    {% if app.user.roles.0 == "ROLE_FREELA" %}

                    {% endif %}
                </div>
            </div>
            <div class="col-md-9">
                {% block body_painel %}
                {% endblock %}
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script type="text/javascript">
        $(document).ready(function(){
            $(".bt_excluir_servico").click(function(ev){
                ev.preventDefault();

                if(confirm("Deseja excluir esse job?")) {
                    var url = $(this).prop("href");
                    window.location = url;
                }
            });
        });
    </script>
{% endblock %}